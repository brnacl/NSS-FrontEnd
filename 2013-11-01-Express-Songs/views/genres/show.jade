extends ../template/layout
include ../mixins/form-helper

block content
  .row
    .small-12.columns
      h3.subheader= genre.name
      table#songs
        thead
          tr
            th Song
        tbody
          each song in genre.songs
            tr
              td: a(href='/songs/#{song.id}')= song.title
